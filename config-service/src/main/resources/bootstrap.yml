server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: ${GIT_REPO}}
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
          default-label: "development"
  security:
    user:
      name: root
      password: s3cr3t